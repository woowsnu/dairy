import React from "react";
import UserCalendar from "react-calendar";

const Calendar = () => {
    //   const [value, onChange] = useState(new Date());
  return (
    <UserCalendar />
  )
}

export default Calendar

{/* <div>{moment(value).format("YYYY년 MM월 DD일")}</div> */}